import { createTheme as createMaterialTheme } from '@mui/material';

export const theme = createMaterialTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#3f51b5'
    },
    secondary: {
      main: '#f50057'
    }
  }
});
